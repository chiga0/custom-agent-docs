{
  "name": "custom-agent-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:links": "markdown-link-check 'docs/**/*.md' --quiet || true",
    "lint:md": "markdownlint 'docs/**/*.md' --config .markdownlint.json"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/starlight": "^0.30.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "markdown-link-check": "^3.12.0",
    "markdownlint-cli": "^0.42.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
